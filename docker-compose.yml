version: '3.8'

services:
  prevencion-incendios:
    build: .
    container_name: prevencion-incendios-app
    ports:
      - "5000:5000"
    environment:
      - DOCKER_ENV=1
    volumes:
      - ./data:/app/data
      - ./Output:/app/Output
    restart: unless-stopped
