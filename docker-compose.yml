version: '3.8'

services:
  prediccion-incendios:
    build: .
    container_name: prediccion-incendios-app
    ports:
      - "5000:5000"
    environment:
      - DOCKER_ENV=1
    volumes:
      - ./data:/app/data
      - ./Output:/app/Output
    restart: unless-stopped
